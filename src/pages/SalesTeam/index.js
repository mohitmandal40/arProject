import React from "react";
import { NavLink } from "react-router-dom";

const SalesTeam = () => {
	return (
		<>
			<div>Sales Teams Page</div>
			<NavLink to="/settings">Go to Settings</NavLink>
		</>
	);
};

export default SalesTeam;
